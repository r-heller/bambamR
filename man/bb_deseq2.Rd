% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/de_analysis.R
\name{bb_deseq2}
\alias{bb_deseq2}
\title{Differential Expression with DESeq2}
\usage{
bb_deseq2(counts, coldata, design = ~condition, contrast = NULL, alpha = 0.05)
}
\arguments{
\item{counts}{Numeric matrix. Raw (unnormalized) count matrix.}

\item{coldata}{A data.frame with sample information. Rownames must match
column names of \code{counts}. Must contain the variables referenced in
\code{design}.}

\item{design}{A formula specifying the design. Default \code{~ condition}.}

\item{contrast}{Character vector of length 3: \code{c("variable", "numerator", "denominator")}. If \code{NULL}, the last variable in the design is used.}

\item{alpha}{Numeric. FDR threshold for independent filtering.
Default \code{0.05}.}
}
\value{
A data.frame with columns \code{gene}, \code{log2fc}, \code{pvalue}, \code{padj},
\code{basemean}.
}
\description{
Runs DESeq2 differential expression analysis. Requires the \code{DESeq2} package.
}
\examples{
\donttest{
if (requireNamespace("DESeq2", quietly = TRUE)) {
  counts <- matrix(rpois(600, 100), nrow = 100, ncol = 6,
    dimnames = list(paste0("gene", 1:100), paste0("S", 1:6)))
  coldata <- data.frame(
    condition = factor(rep(c("ctrl", "treat"), each = 3)),
    row.names = paste0("S", 1:6)
  )
  result <- bb_deseq2(counts, coldata)
  head(result)
}
}

}

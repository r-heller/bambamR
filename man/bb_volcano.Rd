% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_volcano.R
\name{bb_volcano}
\alias{bb_volcano}
\title{Volcano Plot}
\usage{
bb_volcano(
  de_result,
  fc_cutoff = 1,
  p_cutoff = 0.05,
  label_genes = NULL,
  n_label = 10L,
  point_size = 1,
  colors = c(up = "#D73027", down = "#4575B4", ns = "grey70")
)
}
\arguments{
\item{de_result}{A data.frame with columns \code{gene}, \code{log2fc}, \code{pvalue}, \code{padj}.
Typically the output of \code{\link[=bb_deseq2]{bb_deseq2()}}, \code{\link[=bb_edger]{bb_edger()}}, or \code{\link[=bb_limma_voom]{bb_limma_voom()}}.}

\item{fc_cutoff}{Numeric. Absolute log2 fold-change cutoff for significance.
Default \code{1}.}

\item{p_cutoff}{Numeric. Adjusted p-value cutoff for significance.
Default \code{0.05}.}

\item{label_genes}{Character vector. Specific gene names to label on the
plot. If \code{NULL} (default), the top \code{n_label} significant genes are labeled.}

\item{n_label}{Integer. Number of top significant genes to auto-label
when \code{label_genes} is \code{NULL}. Default \code{10}.}

\item{point_size}{Numeric. Size of points. Default \code{1}.}

\item{colors}{Named character vector of length 3 for up, down, and
non-significant colors.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Creates a publication-ready volcano plot from differential expression results.
}
\examples{
de <- data.frame(
  gene = paste0("gene", 1:200),
  log2fc = rnorm(200, 0, 2),
  pvalue = 10^(-runif(200, 0, 5)),
  padj = 10^(-runif(200, 0, 4))
)
bb_volcano(de)

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/de_analysis.R
\name{bb_edger}
\alias{bb_edger}
\title{Differential Expression with edgeR}
\usage{
bb_edger(counts, group, design = NULL)
}
\arguments{
\item{counts}{Numeric matrix. Raw count matrix.}

\item{group}{Factor or character vector. Group assignment for each sample.}

\item{design}{A design matrix. If \code{NULL}, a simple group design is created.}
}
\value{
A data.frame with columns \code{gene}, \code{log2fc}, \code{pvalue}, \code{padj}.
}
\description{
Runs edgeR quasi-likelihood differential expression analysis. Requires
the \code{edgeR} package.
}
\examples{
\donttest{
if (requireNamespace("edgeR", quietly = TRUE)) {
  counts <- matrix(rpois(600, 100), nrow = 100, ncol = 6,
    dimnames = list(paste0("gene", 1:100), paste0("S", 1:6)))
  group <- factor(rep(c("ctrl", "treat"), each = 3))
  result <- bb_edger(counts, group)
  head(result)
}
}

}

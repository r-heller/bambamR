% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/de_analysis.R
\name{bb_limma_voom}
\alias{bb_limma_voom}
\title{Differential Expression with limma-voom}
\usage{
bb_limma_voom(counts, design, contrast = NULL)
}
\arguments{
\item{counts}{Numeric matrix. Raw count matrix.}

\item{design}{A design matrix (e.g., from \code{\link[stats:model.matrix]{stats::model.matrix()}}).}

\item{contrast}{A contrast vector or matrix. If \code{NULL}, the last
coefficient is tested.}
}
\value{
A data.frame with columns \code{gene}, \code{log2fc}, \code{pvalue}, \code{padj}.
}
\description{
Runs limma-voom differential expression analysis. Requires the \code{limma}
and \code{edgeR} packages.
}
\examples{
\donttest{
if (requireNamespace("limma", quietly = TRUE) &&
    requireNamespace("edgeR", quietly = TRUE)) {
  counts <- matrix(rpois(600, 100), nrow = 100, ncol = 6,
    dimnames = list(paste0("gene", 1:100), paste0("S", 1:6)))
  group <- factor(rep(c("ctrl", "treat"), each = 3))
  design <- model.matrix(~ group)
  result <- bb_limma_voom(counts, design)
  head(result)
}
}

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize.R
\name{bb_normalize}
\alias{bb_normalize}
\title{Normalize Count Matrix}
\usage{
bb_normalize(
  counts,
  method = c("cpm", "tpm", "tmm", "rle"),
  gene_lengths = NULL
)
}
\arguments{
\item{counts}{Numeric matrix. Raw count matrix with gene IDs as rownames
and sample names as colnames.}

\item{method}{Character. One of \code{"cpm"}, \code{"tpm"}, \code{"tmm"}, \code{"rle"}.}

\item{gene_lengths}{Numeric vector. Gene lengths in base pairs. Required
for TPM normalization. Must be named or in the same order as rownames
of \code{counts}.}
}
\value{
A normalized numeric matrix with the same dimensions as \code{counts}.
}
\description{
Supports CPM, TPM, TMM (edgeR), and RLE (DESeq2) normalization.
CPM and TPM are always available. TMM requires edgeR, RLE requires DESeq2.
}
\details{
\itemize{
\item \strong{CPM} (Counts Per Million): \code{counts / library_size * 1e6}. Pure base R.
\item \strong{TPM} (Transcripts Per Million): Normalizes by gene length then library
size. Pure base R but requires \code{gene_lengths}.
\item \strong{TMM} (Trimmed Mean of M-values): Uses \code{edgeR::calcNormFactors()}.
Requires the \code{edgeR} package.
\item \strong{RLE} (Relative Log Expression): Uses \code{DESeq2::estimateSizeFactors()}.
Requires the \code{DESeq2} package.
}
}
\examples{
counts <- matrix(
  rpois(600, lambda = 100),
  nrow = 100, ncol = 6,
  dimnames = list(paste0("gene", 1:100), paste0("sample", 1:6))
)
cpm <- bb_normalize(counts, method = "cpm")
head(cpm)

# TPM requires gene lengths
gene_lengths <- sample(500:5000, 100)
tpm <- bb_normalize(counts, method = "tpm", gene_lengths = gene_lengths)

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_fastq.R
\name{bb_read_fastq}
\alias{bb_read_fastq}
\title{Read FASTQ Files}
\usage{
bb_read_fastq(path, n = NULL)
}
\arguments{
\item{path}{Character. Path to a FASTQ or FASTQ.gz file.}

\item{n}{Integer or NULL. Number of reads to sample. \code{NULL} reads all.}
}
\value{
A data.frame with columns:
\describe{
\item{id}{Read identifier}
\item{sequence}{Nucleotide sequence}
\item{quality}{Quality string (Phred+33 encoded)}
}
}
\description{
Reads FASTQ files using ShortRead (if available) or a minimal
base-R parser as fallback. Supports \code{.gz} compressed files.
}
\examples{
\donttest{
# Create a temporary FASTQ file
tmp <- tempfile(fileext = ".fastq")
writeLines(c(
  "@read1", "ACGTACGT", "+", "IIIIIIII",
  "@read2", "TGCATGCA", "+", "HHHHHHHH"
), tmp)
reads <- bb_read_fastq(tmp)
reads
}

}

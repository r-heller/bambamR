% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{bb_pipeline}
\alias{bb_pipeline}
\title{Run Full bambamR Pipeline}
\usage{
bb_pipeline(
  fastq_dir = NULL,
  bam_dir = NULL,
  count_matrix = NULL,
  output_dir = "bambamR_output",
  genome_index = NULL,
  annotation = NULL,
  sample_info = NULL,
  aligner = "STAR",
  de_method = c("DESeq2", "edgeR", "limma"),
  design = ~condition,
  skip = character(0),
  threads = 4L
)
}
\arguments{
\item{fastq_dir}{Character or NULL. Directory containing FASTQ files.
If provided, the pipeline starts from alignment.}

\item{bam_dir}{Character or NULL. Directory containing BAM files.
If provided, the pipeline starts from read counting.}

\item{count_matrix}{A numeric matrix or NULL. Pre-computed count matrix.
If provided, the pipeline starts from normalization/DE.}

\item{output_dir}{Character. Output directory for results.
Default \code{"bambamR_output"}.}

\item{genome_index}{Character or NULL. Path to genome index. Required if
starting from FASTQ.}

\item{annotation}{Character or NULL. Path to GTF annotation. Required if
starting from FASTQ or BAM.}

\item{sample_info}{A data.frame with sample metadata. Must contain a
\code{condition} column (or the variable in \code{design}). Rownames should
match sample identifiers.}

\item{aligner}{Character. Aligner for FASTQ alignment. Default \code{"STAR"}.}

\item{de_method}{Character. DE method: \code{"DESeq2"}, \code{"edgeR"}, or
\code{"limma"}. Default \code{"DESeq2"}.}

\item{design}{A formula for DE analysis. Default \code{~ condition}.}

\item{skip}{Character vector. Steps to skip: \code{"qc"}, \code{"align"},
\code{"count"}, \code{"de"}, \code{"viz"}. Default \code{character(0)}.}

\item{threads}{Integer. Number of threads. Default \code{4}.}
}
\value{
A \code{bb_result} object containing counts, metadata, DE results,
and plots.
}
\description{
Orchestrates the complete RNA-seq analysis pipeline from FASTQ/BAM files
or a count matrix through alignment, counting, normalization, differential
expression, and visualization.
}
\details{
The pipeline allows entry at any stage:
\itemize{
\item \strong{From FASTQ}: requires \code{fastq_dir}, \code{genome_index}, \code{annotation},
and \code{sample_info}
\item \strong{From BAM}: requires \code{bam_dir}, \code{annotation}, and \code{sample_info}
\item \strong{From counts}: requires \code{count_matrix} and \code{sample_info}
}
}
\examples{
\donttest{
# Starting from a count matrix (minimal mode, no Bioconductor needed)
set.seed(42)
counts <- matrix(rpois(600, 100), nrow = 100, ncol = 6,
  dimnames = list(paste0("gene", 1:100), paste0("S", 1:6)))
sample_info <- data.frame(
  condition = factor(rep(c("ctrl", "treat"), each = 3)),
  row.names = paste0("S", 1:6)
)
# result <- bb_pipeline(count_matrix = counts, sample_info = sample_info)
}

}
